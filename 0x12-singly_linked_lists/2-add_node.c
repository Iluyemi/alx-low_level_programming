#include "lists.h"
#include <stdio.h>
list_t *add_node(list_t **head, const char *str)
{
	strdup
list_t head []= list_t *head;
(*head).str = str;
(*head).len = strlen(str);
while (head != NULL)
{
	printf("[%d] %s", (*head).len (*head).str);
	(*head).next;
}



