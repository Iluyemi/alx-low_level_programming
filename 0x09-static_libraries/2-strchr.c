#include "main.h"
#include <string.h>
/**
 * _strchr - My prototype function
 * @c: char to be located
 * @s: array of char
 * Return: on Success
 */
char *_strchr(char *s, char c)
{
int i;
int f;
int length;
char *n;
i = 0;
while (s[i] = c)
{
f = i;
}
length = 0;
while (s[length])
{
length++;
}

for (i = f; i < length; i++)
{
s[i] = n[i];
}
return (n);
}

