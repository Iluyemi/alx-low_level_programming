#include "main.h"
#include <string.h>
/**
 * @dest: character
 * @src: a character
 * Return: if executed properly
 */
char *_strcat(char *dest, char *src)
{
char *s;
s = strcat(dest, src);
return (s);
}
