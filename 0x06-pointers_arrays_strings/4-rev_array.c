#include "main.h"
#include <string.h>
/**
 * _strncpy - print a copy of character
 * @dest: first param
 * @src: second param
 * @n: 3rd param
 * Return: string
 */
void reverse_array(int *a, int n)
{
int f;
for (f = n; f <= n; f--)
{
printf("%d", a[f]);	
}
return;
}
