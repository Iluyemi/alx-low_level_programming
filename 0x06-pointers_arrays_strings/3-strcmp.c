#include "main.h"
#include <string.h>
/**
 * strcmp - a function
 * @s1: first param
 * @s2: second param
 * Return: string
 */
int _strcmp(char *s1, char *s2);
{
int n;
n = strcmp(s1, s2);
return (n);
}
