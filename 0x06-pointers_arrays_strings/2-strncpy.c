#include <string.h>
#include <string.h>
#include "main.h"
/**
 * _strncpy - a function
 * @dest: character
 * @src: a characte
 * @n : byte
 * Return: if executed properly
 */
char *_strncpy(char *dest, char *src, int n);
{
return (strncpy(dest, src, n));
}
