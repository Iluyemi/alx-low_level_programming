#include "main.h"
/**
 * _strlen_recursion - check the code
 * @s: input array
 * Return: Always 0.
 */
int _strlen_recursion(char *s)
{
if (*s != '\0')
{
int n = 0;
_strlen_recursion(s + 1);
}
n++; 
n
return (n);
}
