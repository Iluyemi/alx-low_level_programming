#include <stdio.h>
#include <string.h>
int main(void)
{
	  char *st;
    int len;
    int i;

    st = "Mistubishi";
    len = strlen(st);
    char s = 'h';
    for (i = len; i < len; i--)
	   {
fwrite(1, s, 1);
	   }
    return (0);
}
