#include "main.h"
#include <string.h>
/**
 * reset_to_98 - Updates the value of an int
 * Return: if successful
 * @n: a pointer
 */
void print_rev(char *s)
{
int len;
len = strlen(s);
int i;
for (i = len; i <= len; i--)
{
write(1, "%C, s[i]", 1);
}
return;
}
