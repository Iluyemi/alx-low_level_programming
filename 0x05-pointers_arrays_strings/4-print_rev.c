#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * reset_to_98 - Updates the value of an int
 * Return: if successful
 * @n: a pointer
 */
void print_rev(char *s)
{
int len;
int i;
len = 0;
while (s[n] != '\0')
n++;
for (i = len - 1; i >= 0; i--)
{
printf("%c", s[i]);
}
printf("\n");
return;
}
